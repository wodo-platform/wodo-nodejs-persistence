{
    "name": "@wodo-platform/wodo-nodejs-persistence",
    "version": "1.0.13",
    "description": "Shared prisma client for all nodejs based microservices,",
    "main": "index.js",
    "files": [ 
        "dist/prisma/client/*" 
    ],
    "scripts": {
        "db:introspect": "dotenv -- prisma introspect",
        "db:generate": "dotenv -- prisma generate",
        "test": "echo \"Error: no test specified\" && exit 0",
        "lib-generate": "npx prisma generate && cp -r prisma dist/. "
    },
    "keywords": [],
    "author": "wodo-platform",
    "repository": "git@github.com:wodo-platform/wodo-nodejs-persistence.git",
    "publishConfig": {
        "registry": "https://npm.pkg.github.com"
    },
    "license": "ISC",
    "dependencies": {
        "@prisma/client": "^3.2.1"
    },
    "devDependencies": {
        "@types/node": "^15.12.5",
        "dotenv-cli": "^4.0.0",
        "prisma": "^3.2.1",
        "ts-node": "^10.0.0",
        "typescript": "^4.3.4"
    }
}
